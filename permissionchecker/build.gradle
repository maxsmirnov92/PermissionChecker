apply from: "$rootDir/buildSrc/androidLib.gradle"

android {
    namespace 'net.maxsmr.permissionchecker'
}

dependencies {
    // Support
    implementation "androidx.core:core-ktx:$androidxCoreVersion"

    // EasyPermissions
    api "pub.devrel:easypermissions:$easyPermissionsVersion"
}